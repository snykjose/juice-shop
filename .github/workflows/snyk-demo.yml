# name: Snyk Demo - Quick Start

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   snyk-quick-start:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Run Snyk Open Source test
#         uses: snyk/actions/node@master
#         with:
#           args: --all-projects
#         env:
#           SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

# name: Snyk SCA Demo - CLI Standalone

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   snyk-cli-standalone:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'
#       - name: Install Snyk CLI and run SCA test
#         env:
#           SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
#         run: |
#           npm install -g snyk
#           snyk test --all-projects

name: Snyk SCA Demo - Hybrid Scan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  snyk-hybrid-scan:
    runs-on: ubuntu-latest
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - uses: actions/checkout@v3
      - name: Run Snyk Open Source test
        uses: snyk/actions/node@master
      - name: Run Snyk Open Source monitor
        run: |
          npm install -g snyk
          snyk test --all-projects
